                                            <div key={field.key} className="bg-slate-50 dark:bg-slate-900/60 border border-slate-200 dark:border-slate-700 rounded-2xl p-4 transition-all duration-300 hover:border-emerald-400 focus-within:border-emerald-500 focus-within:ring-2 focus-within:ring-emerald-500/20">
                                                <label className="block text-xs font-bold text-slate-500 dark:text-slate-400 uppercase tracking-wide mb-1">
                                                    <Icon name={field.icon} className={`mr-1 ${isHole ? 'text-hole-text' : 'text-shaft-text'}`} />
                                                    {field.label}
                                                </label>
                                                <div className="text-[10px] text-slate-400 dark:text-slate-500 mb-2">{field.sub}</div>
                                                <input
                                                    type="number"
                                                    step="any"
                                                    value={formData[field.key]}
                                                    onChange={e => setFormData(prev => ({ ...prev, [field.key]: e.target.value }))}
                                                    placeholder="Âµm"
                                                    className={`w-full bg-white dark:bg-slate-800 border ${isHole ? 'border-hole-border/60 focus:ring-hole-border/40' : 'border-shaft-border/60 focus:ring-shaft-border/40'} rounded-xl p-3 text-slate-900 dark:text-white font-mono font-bold text-center outline-none focus:ring-2 transition-all duration-300 shadow-sm placeholder:text-slate-300 dark:placeholder:text-slate-600`}
                                                />
                                            </div>
                                        );
                                    })}

                                    {error && (
                                        <div className="col-span-2 lg:col-span-4 bg-rose-50 dark:bg-rose-950/40 border border-rose-300 dark:border-rose-800 border-l-4 border-l-rose-500 text-rose-700 dark:text-rose-300 px-4 py-3 rounded-xl text-sm flex items-start gap-2.5" role="alert">
                                            <Icon name="circle-exclamation" className="mt-0.5 shrink-0" />
                                            <span>{error}</span>
                                        </div>
                                    )}

                                    <div className="col-span-2 lg:col-span-4 flex flex-col sm:flex-row gap-3 pt-1">
                                        <button
                                            type="submit"
                                            disabled={submitting}
                                            className="flex-1 bg-gradient-to-r from-emerald-600 to-teal-600 hover:from-emerald-500 hover:to-teal-500 disabled:opacity-70 disabled:cursor-not-allowed text-white font-bold rounded-xl p-4 transition-all duration-300 flex items-center justify-center gap-2 shadow-soft hover:shadow-primary-glow hover:scale-[1.01] active:scale-[0.99] tracking-wide"
                                        >
                                            {submitting ? <Icon name="spinner" className="fa-spin" /> : <Icon name="paper-plane" />}
                                            {submitting ? 'Äang cháº¥m bÃ i...' : 'Ná»˜P BÃ€I'}
                                        </button>
                                        <button
                                            type="button"
                                            onClick={handleReset}
                                            disabled={submitting}
                                            className="sm:w-44 bg-slate-100 hover:bg-slate-200 dark:bg-slate-800 dark:hover:bg-slate-700 text-slate-700 dark:text-slate-200 px-4 py-2.5 rounded-xl font-semibold transition-all duration-300 flex items-center justify-center gap-2 border border-slate-200 dark:border-slate-700 hover:scale-[1.02] active:scale-95 text-sm disabled:opacity-60"
                                        >
                                            <Icon name="eraser" /> Nháº­p láº¡i
                                        </button>
                                    </div>
                                </form>
                            </div>
                        </Card>
                    )}

                    {/* ====== THáºº Káº¾T QUáº¢ (RESULT CARD) ====== */}
                    {isSubmitted && result && (
                        <Card className={`overflow-hidden border-0 ${result.score >= 8 ? 'border-t-4 border-t-emerald-500' : result.score >= 5 ? 'border-t-4 border-t-amber-500' : 'border-t-4 border-t-rose-500'}`}>
                            {/* Header Ä‘iá»ƒm */}
                            <div className={`bg-gradient-to-br p-6 lg:p-8 relative overflow-hidden ${result.score >= 8 ? 'from-emerald-500 via-emerald-600 to-teal-700' : result.score >= 5 ? 'from-amber-500 via-amber-600 to-orange-700' : 'from-rose-500 via-rose-600 to-red-700'}`}>
                                {/* Decorative circles */}
                                <div className="absolute -top-10 -right-10 w-40 h-40 rounded-full bg-white/10"></div>
                                <div className="absolute -bottom-16 -left-10 w-52 h-52 rounded-full bg-black/10"></div>
                                
                                <div className="relative flex flex-col lg:flex-row lg:items-center gap-6">
                                    <div className="flex-1 text-white">
                                        <div className="flex items-center gap-2 mb-1.5">
                                            <span className="bg-white/20 backdrop-blur px-3 py-1 rounded-full text-xs font-bold uppercase tracking-wider">
                                                <Icon name={result.already_submitted ? 'circle-check' : 'trophy'} className="mr-1" />
                                                Báº¡n Ä‘Ã£ hoÃ n thÃ nh bÃ i táº­p
                                            </span>
                                        </div>
                                        <h3 className="text-2xl lg:text-3xl font-bold tracking-tight drop-shadow-md">
                                            Káº¾T QUáº¢ CHáº¤M ÄIá»‚M Tá»° Äá»˜NG
                                        </h3>
                                        <p className="text-white/80 text-sm mt-2 flex items-center gap-2">
                                            <Icon name="file-lines" /> MÃ£ Ä‘á»: <span className="font-mono font-bold">{a ? a.examCode : 'â€”'}</span>
                                            {a && <span className="ml-2 font-mono">Ï•{a.D} {hole.sym}{hole.it}/{shaft.sym}{shaft.it}</span>}
                                        </p>
                                    </div>

                                    {/* Äiá»ƒm sá»‘ lá»›n */}
                                    <div className="flex items-center gap-5">
                                        <div className="bg-white/20 backdrop-blur-md rounded-2xl px-8 py-5 text-center shadow-xl border border-white/20">
                                            <div className="text-5xl lg:text-6xl font-bold text-white drop-shadow-md font-mono">{result.score}</div>
                                            <div className="text-white/90 text-sm font-semibold tracking-wider mt-1">/ 10.0 ÄIá»‚M</div>
                                        </div>
                                        <div className="text-center text-white">
                                            <div className="w-16 h-16 mx-auto rounded-full bg-white/20 border-2 border-white/40 flex items-center justify-center text-3xl mb-1">
                                                <Icon name={result.score >= 8 ? 'face-smile' : result.score >= 5 ? 'face-meh' : 'face-frown'} />
                                            </div>
                                            <div className="text-xs font-semibold uppercase tracking-wider text-white/85">
                                                {result.score >= 8 ? 'Xuáº¥t sáº¯c!' : result.score >= 5 ? 'Äáº¡t yÃªu cáº§u' : 'Cáº§n cá»‘ gáº¯ng'}
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            {/* Báº£ng Ä‘á»‘i chiáº¿u ÄÃ¡p Ã¡n cá»§a báº¡n | ÄÃ¡p Ã¡n Ä‘Ãºng | Káº¿t quáº£ */}
                            <div className="p-6 lg:p-8 bg-white dark:bg-slate-800/80">
                                <h4 className="font-bold text-lg text-slate-800 dark:text-white mb-4 flex items-center gap-2">
                                    <Icon name="table-list" className="text-primary-500" /> Äá»‘i Chiáº¿u Chi Tiáº¿t Tá»«ng ThÃ´ng Sá»‘
                                </h4>

                                <div className="overflow-x-auto rounded-xl border border-slate-200 dark:border-slate-700">
                                    <table className="w-full text-sm">
                                        <thead>
                                            <tr className="bg-slate-100 dark:bg-slate-900 text-slate-700 dark:text-slate-200 text-left">
                                                <th className="border-b border-slate-200 dark:border-slate-700 p-3.5 font-bold">ThÃ´ng sá»‘</th>
                                                <th className="border-b border-slate-200 dark:border-slate-700 p-3.5 font-bold text-center">ÄÃ¡p Ã¡n cá»§a báº¡n</th>
                                                <th className="border-b border-slate-200 dark:border-slate-700 p-3.5 font-bold text-center">ÄÃ¡p Ã¡n Ä‘Ãºng</th>
                                                <th className="border-b border-slate-200 dark:border-slate-700 p-3.5 font-bold text-center">Káº¿t quáº£</th>
                                                <th className="border-b border-slate-200 dark:border-slate-700 p-3.5 font-bold text-center">Äiá»ƒm</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            {(result.details || []).map((item, idx) => (
                                                <tr key={item.key} className={`border-b border-slate-100 dark:border-slate-800 ${item.is_correct ? 'bg-emerald-50/50 dark:bg-emerald-950/20' : 'bg-rose-50/50 dark:bg-rose-950/20'}`}>
                                                    <td className="p-3.5 font-medium text-slate-800 dark:text-slate-200">
                                                        <span className="font-mono font-bold">{item.key}</span>
                                                        <span className="text-xs text-slate-500 dark:text-slate-400 ml-2 hidden md:inline">{item.label}</span>
                                                    </td>
                                                    <td className="p-3.5 text-center font-mono font-bold text-slate-800 dark:text-white">
                                                        {Number(item.student) > 0 ? `+${item.student}` : item.student} Âµm
                                                    </td>
                                                    <td className="p-3.5 text-center font-mono font-bold text-slate-800 dark:text-white">
                                                        {Number(item.correct) > 0 ? `+${item.correct}` : item.correct} Âµm
                                                    </td>
                                                    <td className="p-3.5 text-center">
                                                        {item.is_correct ? (
                                                            <span className="inline-flex items-center gap-1.5 bg-emerald-100 dark:bg-emerald-950/50 text-emerald-700 dark:text-emerald-300 px-3 py-1 rounded-full font-bold text-xs border border-emerald-300 dark:border-emerald-800">
                                                                <Icon name="check" /> ÄÃºng
                                                            </span>
                                                        ) : (
                                                            <span className="inline-flex items-center gap-1.5 bg-rose-100 dark:bg-rose-950/50 text-rose-700 dark:text-rose-300 px-3 py-1 rounded-full font-bold text-xs border border-rose-300 dark:border-rose-800">
                                                                <Icon name="xmark" /> Sai
                                                            </span>
                                                        )}
                                                    </td>
                                                    <td className="p-3.5 text-center font-mono font-bold text-slate-700 dark:text-slate-300">
                                                        {item.is_correct ? '2.5' : '0.0'}
                                                    </td>
                                                </tr>
                                            ))}
                                        </tbody>
                                        <tfoot>
                                            <tr className="bg-slate-100 dark:bg-slate-900 font-bold text-slate-900 dark:text-white">
                                                <td colSpan="4" className="p-4 text-right uppercase tracking-wider text-sm">Tá»•ng Ä‘iá»ƒm:</td>
                                                <td className="p-4 text-center font-mono text-lg text-emerald-600 dark:text-emerald-400">{result.score} / 10.0</td>
                                            </tr>
                                        </tfoot>
                                    </table>
                                </div>

                                {/* Äá» bÃ i + ÄÃ¡p Ã¡n Ä‘Ãºng tham kháº£o */}
                                {hole && shaft && (
                                    <div className="mt-6 grid grid-cols-1 md:grid-cols-2 gap-4">
                                        <div className="bg-gradient-to-br from-sky-50 to-blue-50 dark:from-sky-950/30 dark:to-blue-950/15 border border-sky-200 dark:border-sky-800/50 rounded-xl p-4">
                                            <div className="font-bold text-sm text-hole-text mb-2"><Icon name="circle-dot" className="mr-1" /> Lá»— Ï•{a.D} {hole.sym}{hole.it}</div>
                                            <div className="font-mono text-xs text-slate-600 dark:text-slate-300 space-y-1">
                                                <div>ES = {hole.ES > 0 ? `+${hole.ES}` : hole.ES} Âµm Â· EI = {hole.EI} Âµm</div>
                                                <div>Dmax = {hole.Dmax.toFixed(3)} mm Â· Dmin = {hole.Dmin.toFixed(3)} mm</div>
                                            </div>
                                        </div>
                                        <div className="bg-gradient-to-br from-orange-50 to-amber-50 dark:from-orange-950/30 dark:to-amber-950/15 border border-orange-200 dark:border-orange-800/50 rounded-xl p-4">
                                            <div className="font-bold text-sm text-shaft-text mb-2"><Icon name="circle" className="mr-1" /> Trá»¥c Ï•{a.D} {shaft.sym}{shaft.it}</div>
                                            <div className="font-mono text-xs text-slate-600 dark:text-slate-300 space-y-1">
                                                <div>es = {shaft.es > 0 ? `+${shaft.es}` : shaft.es} Âµm Â· ei = {shaft.ei > 0 ? `+${shaft.ei}` : shaft.ei} Âµm</div>
                                                <div>dmax = {shaft.dmax.toFixed(3)} mm Â· dmin = {shaft.dmin.toFixed(3)} mm</div>
                                            </div>
                                        </div>
                                    </div>
                                )}

                                <div className="mt-6 flex flex-col sm:flex-row items-center justify-between gap-4 bg-slate-50 dark:bg-slate-900/60 border border-slate-200 dark:border-slate-700 rounded-xl p-4">
                                    <p className="text-sm text-slate-600 dark:text-slate-300 flex items-center gap-2">
                                        <Icon name="circle-info" className="text-primary-500" />
                                        BÃ i táº­p Ä‘Ã£ hoÃ n thÃ nh. Báº¡n khÃ´ng thá»ƒ ná»™p láº¡i láº§n hai.
                                    </p>
                                    <button
                                        onClick={() => window.print()}
                                        className="bg-slate-100 hover:bg-slate-200 dark:bg-slate-800 dark:hover:bg-slate-700 text-slate-700 dark:text-slate-200 px-4 py-2.5 rounded-xl font-semibold transition-all duration-300 flex items-center gap-2 border border-slate-200 dark:border-slate-700 hover:scale-[1.03] active:scale-95 text-sm shrink-0"
                                    >
                                        <Icon name="print" /> In káº¿t quáº£
                                    </button>
                                </div>
                            </div>
                        </Card>
                    )}
                </div>
            );
        };

        // ===================================================================
        // LOGIN SCREEN - MÃ n hÃ¬nh Ä‘Äƒng nháº­p (JWT + RBAC)
        // ===================================================================
        const LoginScreen = ({ onLogin, theme, toggleTheme }) => {
            const [username, setUsername] = useState('');
            const [password, setPassword] = useState('');
            const [showPassword, setShowPassword] = useState(false);
            const [remember, setRemember] = useState(true);
            const [loading, setLoading] = useState(false);
            const [error, setError] = useState('');

            const handleSubmit = async (e) => {
                e.preventDefault();
                if (!username.trim() || !password) {
                    setError('Vui lÃ²ng nháº­p Ä‘áº§y Ä‘á»§ tÃªn Ä‘Äƒng nháº­p vÃ  máº­t kháº©u.');
                    return;
                }
                setLoading(true);
                setError('');
                try {
                    const res = await fetch('http://127.0.0.1:8000/api/login', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({ username: username.trim(), password })
                    });
                    const data = await res.json().catch(() => ({}));
                    if (!res.ok) {
                        throw new Error(data.detail || 'ÄÄƒng nháº­p tháº¥t báº¡i. Vui lÃ²ng kiá»ƒm tra láº¡i.');
                    }
                    onLogin(data);
                } catch (err) {
                    console.error('[Login] Lá»—i:', err);
                    setError(err.message || 'KhÃ´ng káº¿t ná»‘i Ä‘Æ°á»£c mÃ¡y chá»§. Vui lÃ²ng cháº¡y backend (uvicorn main:app --reload).');
                } finally {
                    setLoading(false);
                }
            };

            const quickFill = (user, pass) => {
                setUsername(user);
                setPassword(pass);
                setError('');
            };

            return (
                <div className="min-h-screen flex items-center justify-center relative overflow-hidden bg-slate-50 dark:bg-navy-950">
                    {/* Gradient background decoration */}
                    <div className="absolute inset-0 pointer-events-none" aria-hidden="true">
                        <div className="absolute -top-40 -left-40 w-[500px] h-[500px] rounded-full bg-gradient-to-br from-primary-500/20 to-indigo-600/20 blur-3xl"></div>
                        <div className="absolute -bottom-40 -right-40 w-[500px] h-[500px] rounded-full bg-gradient-to-br from-emerald-500/10 to-cyan-500/10 blur-3xl"></div>
                        <div className="absolute top-1/3 left-1/2 -translate-x-1/2 w-[700px] h-[300px] rounded-full bg-gradient-to-r from-primary-500/10 via-indigo-500/10 to-transparent blur-3xl"></div>
                    </div>

                    <div className="relative z-10 w-full max-w-md px-6 animate-fadeInUp">
                        <div className="text-center mb-8">
                            <div className="inline-flex items-center justify-center w-16 h-16 rounded-2xl bg-gradient-to-br from-primary-500 to-indigo-600 text-white text-3xl font-bold font-mono shadow-primary-glow mb-4">
                                IS
                            </div>
                            <h1 className="text-3xl font-bold tracking-tight text-slate-900 dark:text-white">
                                ISO 286 <span className="text-primary-600 dark:text-primary-400">EdTech</span>
                            </h1>
                            <p className="text-sm text-slate-500 dark:text-slate-400 mt-2 leading-relaxed">
                                Há»‡ thá»‘ng Ra Ä‘á» &amp; Tra cá»©u Dung sai<br className="sm:hidden" />
                                <span className="hidden sm:inline">Láº¯p ghÃ©p TiÃªu chuáº©n Quá»‘c táº¿</span>
                            </p>
                        </div>

                        <div className="bg-white/90 dark:bg-slate-800/90 backdrop-blur-xl rounded-2xl shadow-soft border border-slate-100 dark:border-slate-700 p-8">
                            <div className="flex items-center justify-between mb-6">
                                <div>
                                    <h2 className="text-xl font-bold text-slate-900 dark:text-white">ÄÄƒng nháº­p</h2>
                                    <p className="text-xs text-slate-500 dark:text-slate-400 mt-1">Sá»­ dá»¥ng tÃ i khoáº£n cá»§a báº¡n Ä‘á»ƒ tiáº¿p tá»¥c</p>
                                </div>
                                <button
                                    type="button"
                                    onClick={toggleTheme}
                                    className="w-10 h-10 rounded-full flex items-center justify-center bg-slate-100 dark:bg-slate-700 text-slate-600 dark:text-slate-300 hover:bg-slate-200 dark:hover:bg-slate-600 hover:scale-110 active:scale-95 transition-all duration-300 shadow-sm shrink-0"
                                    title={theme === 'light' ? 'Chuyá»ƒn sang cháº¿ Ä‘á»™ tá»‘i' : 'Chuyá»ƒn sang cháº¿ Ä‘á»™ sÃ¡ng'}
                                >
                                    <Icon name={theme === 'light' ? 'moon' : 'sun'} />
                                </button>
                            </div>

                            {error && (
                                <div className="mb-5 bg-rose-50 dark:bg-rose-950/40 border border-rose-300 dark:border-rose-800 border-l-4 border-l-rose-500 text-rose-700 dark:text-rose-300 px-4 py-3 rounded-xl text-sm flex items-start gap-2.5" role="alert">
                                    <Icon name="circle-exclamation" className="mt-0.5 shrink-0" />
                                    <span>{error}</span>
                                </div>
                            )}

                            <form onSubmit={handleSubmit} className="space-y-4">
                                <div>
                                    <label className="block text-sm font-semibold mb-2 text-slate-700 dark:text-slate-300">
                                        TÃªn Ä‘Äƒng nháº­p
                                    </label>
                                    <div className="relative">
                                        <Icon name="user" className="absolute left-3.5 top-3.5 text-slate-400 text-sm" />
                                        <input
                                            type="text"
                                            value={username}
                                            onChange={e => setUsername(e.target.value)}
                                            placeholder="VD: giangvien hoáº·c sinhvien"
                                            autoComplete="username"
                                            className="w-full bg-slate-50 dark:bg-slate-900 border border-slate-200 dark:border-slate-700 rounded-xl pl-10 pr-4 py-3 text-slate-900 dark:text-white outline-none focus:ring-2 focus:ring-primary-500/40 focus:border-primary-500 transition-all duration-300 shadow-sm placeholder:text-slate-400"
                                        />
                                    </div>
                                </div>

                                <div>
                                    <label className="block text-sm font-semibold mb-2 text-slate-700 dark:text-slate-300">
                                        Máº­t kháº©u
                                    </label>
                                    <div className="relative">
                                        <Icon name="lock" className="absolute left-3.5 top-3.5 text-slate-400 text-sm" />
                                        <input
                                            type={showPassword ? 'text' : 'password'}
                                            value={password}
                                            onChange={e => setPassword(e.target.value)}
                                            placeholder="â€¢â€¢â€¢â€¢â€¢â€¢â€¢â€¢"
                                            autoComplete="current-password"
                                            className="w-full bg-slate-50 dark:bg-slate-900 border border-slate-200 dark:border-slate-700 rounded-xl pl-10 pr-12 py-3 text-slate-900 dark:text-white outline-none focus:ring-2 focus:ring-primary-500/40 focus:border-primary-500 transition-all duration-300 shadow-sm placeholder:text-slate-400"
                                        />
                                        <button
                                            type="button"
                                            onClick={() => setShowPassword(s => !s)}
                                            className="absolute right-3 top-2.5 text-slate-400 hover:text-primary-600 transition-colors"
                                            title={showPassword ? 'áº¨n máº­t kháº©u' : 'Hiá»‡n máº­t kháº©u'}
                                        >
                                            <Icon name={showPassword ? 'eye-slash' : 'eye'} />
                                        </button>
                                    </div>
                                </div>

                                <div className="flex items-center justify-between text-sm">
                                    <label className="flex items-center gap-2 cursor-pointer text-slate-600 dark:text-slate-400 select-none">
                                        <input
                                            type="checkbox"
                                            checked={remember}
                                            onChange={e => setRemember(e.target.checked)}
                                            className="w-4 h-4 accent-primary-600 rounded"
                                        />
                                        Ghi nhá»› Ä‘Äƒng nháº­p
                                    </label>
                                    <span className="text-primary-600 dark:text-primary-400 font-medium cursor-pointer hover:underline">
                                        QuÃªn máº­t kháº©u?
                                    </span>
                                </div>

                                <button
                                    type="submit"
                                    disabled={loading}
                                    className="w-full bg-gradient-to-r from-primary-600 to-indigo-600 hover:from-primary-500 hover:to-indigo-500 disabled:opacity-60 disabled:cursor-not-allowed text-white font-bold rounded-xl py-3.5 transition-all duration-300 flex items-center justify-center gap-2 shadow-soft hover:shadow-primary-glow hover:scale-[1.02] active:scale-95 tracking-wide mt-2"
                                >
                                    {loading ? <Icon name="spinner" className="fa-spin" /> : <Icon name="right-to-bracket" />}
                                    {loading ? 'Äang Ä‘Äƒng nháº­p...' : 'ÄÄ‚NG NHáº¬P'}
                                </button>
                            </form>

                            {/* Quick-fill demo accounts */}
                            <div className="mt-6 pt-5 border-t border-slate-200 dark:border-slate-700">
                                <div className="text-[11px] uppercase tracking-widest font-bold text-slate-400 mb-3 text-center">
                                    TÃ i khoáº£n dÃ¹ng thá»­ (máº­t kháº©u: 123)
                                </div>
                                <div className="grid grid-cols-2 gap-3">
                                    {MOCK_USERS.map((user, idx) => {
                                        const isTeacher = user.role === 'teacher';
                                        const subLabel = isTeacher
                                            ? `${user.username} Â· Giáº£ng viÃªn`
                                            : `${user.mssv} Â· ${user.className}`;
                                        return (
                                            <button
                                                key={user.username}
                                                type="button"
                                                onClick={() => quickFill(user.username, user.password)}
                                                className={`group px-3 py-2.5 rounded-xl bg-slate-50 dark:bg-slate-900 border border-slate-200 dark:border-slate-700 transition-all duration-300 text-left ${isTeacher ? 'hover:border-primary-400 hover:bg-primary-50/50 dark:hover:bg-primary-950/20' : 'hover:border-emerald-400 hover:bg-emerald-50/50 dark:hover:bg-emerald-950/20'}`}
                                            >
                                                <div className="flex items-center gap-2">
                                                    <div className={`w-7 h-7 rounded-lg bg-gradient-to-br text-white flex items-center justify-center text-xs shrink-0 ${isTeacher ? 'from-primary-500 to-indigo-600' : 'from-emerald-500 to-teal-600'}`}>
                                                        <Icon name={isTeacher ? 'chalkboard-user' : 'user-graduate'} />
                                                    </div>
                                                    <div className="min-w-0">
                                                        <div className="text-xs font-bold text-slate-800 dark:text-white truncate">{user.full_name}</div>
                                                        <div className="text-[10px] font-mono text-slate-500 truncate">{subLabel}</div>
                                                    </div>
                                                </div>
                                            </button>
                                        );
                                    })}
                                </div>
                                <p className="text-[11px] text-slate-400 dark:text-slate-500 mt-3 text-center leading-relaxed">
                                    Sinh viÃªn khÃ´ng nhÃ¬n tháº¥y tab "Táº¡o Äá» Tá»± Äá»™ng"<br />
                                    vÃ  khÃ´ng thá»ƒ gá»i API xuáº¥t Word/PDF.
                                </p>
                            </div>
                        </div>

                        <p className="text-center text-[11px] text-slate-400 dark:text-slate-600 mt-6 font-mono">
                            ISO 286 EdTech Engine Â· JWT Auth + Role-Based Access Control
                        </p>
                    </div>
                </div>
            );
        };

        const App = () => {
            const [activeMenu, setActiveMenu] = useState('lookup');
            const [theme, setTheme] = useState('light');
            const [currentUser, setCurrentUser] = useState(null);
            const [token, setToken] = useState(null);
            const [checkingSession, setCheckingSession] = useState(true);

            // KhÃ´i phá»¥c phiÃªn Ä‘Äƒng nháº­p tá»« localStorage khi táº£i trang.
            useEffect(() => {
                try {
                    const savedToken = localStorage.getItem('iso286_token');
                    const savedUser = localStorage.getItem('iso286_user');
                    if (savedToken && savedUser) {
                        // Kiá»ƒm tra token cÃ²n háº¡n (exp - pháº§n thá»© 2 cá»§a JWT, Ä‘Æ¡n vá»‹ giÃ¢y).
                        const parts = savedToken.split('.');
                        if (parts.length === 3) {
                            const payload = JSON.parse(atob(parts[1].replace(/-/g, '+').replace(/_/g, '/')));
                            const expMs = payload.exp ? payload.exp * 1000 : 0;
                            if (expMs > Date.now()) {
                                AUTH_TOKEN = savedToken;
                                setToken(savedToken);
                                setCurrentUser(JSON.parse(savedUser));
                            } else {
                                localStorage.removeItem('iso286_token');
                                localStorage.removeItem('iso286_user');
                                AUTH_TOKEN = null;
                            }
                        }
                    }
                } catch (e) {
                    console.warn('[Auth] KhÃ´ng khÃ´i phá»¥c Ä‘Æ°á»£c phiÃªn Ä‘Äƒng nháº­p.', e);
                    localStorage.removeItem('iso286_token');
                    localStorage.removeItem('iso286_user');
                    AUTH_TOKEN = null;
                } finally {
                    setCheckingSession(false);
                }
            }, []);

            useEffect(() => {
                if (theme === 'dark') document.documentElement.classList.add('dark');
                else document.documentElement.classList.remove('dark');
            }, [theme]);

            const toggleTheme = () => setTheme(t => t === 'light' ? 'dark' : 'light');

            const handleLogin = (data) => {
                AUTH_TOKEN = data.access_token;
                setToken(data.access_token);
                setCurrentUser(data.user);
                localStorage.setItem('iso286_token', data.access_token);
                localStorage.setItem('iso286_user', JSON.stringify(data.user));
                setActiveMenu('lookup');
            };

            const handleLogout = () => {
                AUTH_TOKEN = null;
                setToken(null);
                setCurrentUser(null);
                localStorage.removeItem('iso286_token');
                localStorage.removeItem('iso286_user');
                setActiveMenu('lookup');
            };

            const isStudent = currentUser && currentUser.role === 'student';

            // Äang khÃ´i phá»¥c phiÃªn -> hiá»ƒn thá»‹ splash screen.
            if (checkingSession) {
                return (
                    <div className="min-h-screen flex items-center justify-center bg-slate-50 dark:bg-navy-950">
                        <div className="text-center">
                            <div className="w-16 h-16 mx-auto rounded-2xl bg-gradient-to-br from-primary-500 to-indigo-600 text-white flex items-center justify-center text-3xl font-bold font-mono shadow-primary-glow mb-4 animate-pulseSoft">IS</div>
                            <div className="text-sm text-slate-500 dark:text-slate-400 font-medium">Äang khÃ´i phá»¥c phiÃªn Ä‘Äƒng nháº­p...</div>
                        </div>
                    </div>
                );
            }

            // ChÆ°a Ä‘Äƒng nháº­p -> hiá»ƒn thá»‹ mÃ n hÃ¬nh Login.
            if (!currentUser || !token) {
                return <LoginScreen onLogin={handleLogin} theme={theme} toggleTheme={toggleTheme} />;
            }

            const roleLabel = currentUser.role === 'lecturer' ? 'Giáº£ng viÃªn' : 'Sinh viÃªn';

            return (
                <div className="flex h-screen overflow-hidden">
                    <aside className="w-64 bg-gradient-to-b from-slate-900 via-navy-900 to-navy-950 text-slate-300 flex flex-col transition-all duration-300 no-print border-r border-slate-800/60">
                        <div className="p-6 border-b border-slate-800/60 flex items-center gap-3">
                            <div className="w-10 h-10 bg-gradient-to-br from-primary-500 to-indigo-600 rounded-xl flex items-center justify-center text-white font-bold font-mono text-lg shadow-primary-glow">IS</div>
                            <span className="font-bold text-white tracking-wide leading-tight">ISO 286<br/><span className="text-[11px] text-slate-400 font-normal tracking-widest uppercase">EdTech Engine</span></span>
                        </div>
                        <nav className="flex-1 p-4 space-y-1.5">
                            <button 
                                onClick={() => setActiveMenu('lookup')}
                                className={`w-full flex items-center gap-3 px-4 py-3 rounded-xl text-sm font-medium transition-all duration-300 group ${activeMenu === 'lookup' ? 'bg-gradient-to-r from-primary-600 to-indigo-600 text-white shadow-primary-glow' : 'text-slate-400 hover:text-white hover:bg-white/5 hover:translate-x-1'}`}>
                                <Icon name="magnifying-glass-chart" className="w-4 text-center" /> Tra Cá»©u Trá»±c Quan
                            </button>
                            <button 
                                onClick={() => setActiveMenu('learning')}
                                className={`w-full flex items-center gap-3 px-4 py-3 rounded-xl text-sm font-medium transition-all duration-300 group ${activeMenu === 'learning' ? 'bg-gradient-to-r from-primary-600 to-indigo-600 text-white shadow-primary-glow' : 'text-slate-400 hover:text-white hover:bg-white/5 hover:translate-x-1'}`}>
                                <Icon name="graduation-cap" className="w-4 text-center" /> HÆ°á»›ng Dáº«n Há»c Táº­p
                            </button>
                            <button 
                                onClick={() => setActiveMenu('library')}
                                className={`w-full flex items-center gap-3 px-4 py-3 rounded-xl text-sm font-medium transition-all duration-300 group ${activeMenu === 'library' ? 'bg-gradient-to-r from-primary-600 to-indigo-600 text-white shadow-primary-glow' : 'text-slate-400 hover:text-white hover:bg-white/5 hover:translate-x-1'}`}>
                                <Icon name="book-bookmark" className="w-4 text-center" /> ThÆ° Viá»‡n ISO 286
                            </button>
{!isStudent && (
                            <button 
                                onClick={() => setActiveMenu('generate')}
                                className={`w-full flex items-center gap-3 px-4 py-3 rounded-xl text-sm font-medium transition-all duration-300 group ${activeMenu === 'generate' ? 'bg-gradient-to-r from-primary-600 to-indigo-600 text-white shadow-primary-glow' : 'text-slate-400 hover:text-white hover:bg-white/5 hover:translate-x-1'}`}>
                                <Icon name="file-circle-check" className="w-4 text-center" /> Táº¡o Äá» Tá»± Äá»™ng
                            </button>
                            )}
                            {isStudent && (
                            <button 
                                onClick={() => setActiveMenu('assignment')}
                                className={`w-full flex items-center gap-3 px-4 py-3 rounded-xl text-sm font-medium transition-all duration-300 group ${activeMenu === 'assignment' ? 'bg-gradient-to-r from-emerald-600 to-teal-600 text-white shadow-primary-glow' : 'text-slate-400 hover:text-white hover:bg-white/5 hover:translate-x-1'}`}>
                                <Icon name="pen-to-square" className="w-4 text-center" /> BÃ i Táº­p Cá»§a TÃ´i
                            </button>
                            )}
                            <button 
                                onClick={() => setActiveMenu('history')}
                                className={`w-full flex items-center gap-3 px-4 py-3 rounded-xl text-sm font-medium transition-all duration-300 group ${activeMenu === 'history' ? 'bg-gradient-to-r from-primary-600 to-indigo-600 text-white shadow-primary-glow' : 'text-slate-400 hover:text-white hover:bg-white/5 hover:translate-x-1'}`}>
                                <Icon name="clock-rotate-left" className="w-4 text-center" /> Lá»‹ch Sá»­ Äá» Thi
                            </button>
                        </nav>
                        <div className="p-5 border-t border-slate-800/60 text-[11px] text-slate-500 leading-relaxed">
                            <div className="font-mono">v2.2 Standard Rubric</div>
                            <div>Learning Mode Â· ISO 286</div>
                        </div>
                    </aside>

                    <main className="flex-1 flex flex-col h-screen overflow-y-auto print:overflow-visible relative">
                        <header className="h-16 bg-white/80 dark:bg-slate-900/80 border-b border-slate-200/70 dark:border-slate-800/70 backdrop-blur-md flex items-center justify-between px-6 lg:px-10 sticky top-0 z-10 no-print">
                            <div className="flex items-center gap-3">
                                <div className="w-8 h-8 rounded-lg bg-gradient-to-br from-primary-500 to-indigo-600 text-white flex items-center justify-center text-sm lg:hidden">
                                    <Icon name="bars" />
                                </div>
<h1 className="text-lg lg:text-xl font-bold tracking-tight text-slate-800 dark:text-white">
                                    {activeMenu === 'lookup' ? 'Module Giáº£ng Dáº¡y & Trá»±c Quan' : activeMenu === 'learning' ? 'MÃ n HÃ¬nh HÆ°á»›ng Dáº«n Há»c Táº­p (Learning Mode)' : activeMenu === 'library' ? 'Kho Dá»¯ Liá»‡u TiÃªu Chuáº©n Ká»¹ Thuáº­t ISO' : activeMenu === 'history' ? 'Lá»‹ch Sá»­ Äá» Thi ÄÃ£ Táº¡o' : activeMenu === 'assignment' ? 'BÃ i Táº­p Cá»§a TÃ´i - LÃ m & Ná»™p BÃ i' : 'Module Sinh Äá» & Cháº¥m Tá»± Äá»™ng (Rubric 10.0)'}
                                </h1>
                            </div>
                            <div className="flex items-center gap-4">
                                <button onClick={toggleTheme} className="w-10 h-10 rounded-full flex items-center justify-center bg-slate-100 dark:bg-slate-800 text-slate-600 dark:text-slate-300 hover:bg-slate-200 dark:hover:bg-slate-700 hover:scale-110 active:scale-95 transition-all duration-300 shadow-sm">
                                    <Icon name={theme === 'light' ? 'moon' : 'sun'} />
                                </button>
                                <div className="flex items-center gap-3 pl-4 border-l border-slate-200 dark:border-slate-700">
                                    <div className="relative">
                                        <div className={`w-10 h-10 rounded-full flex items-center justify-center text-white font-bold shadow-md ${currentUser.role === 'lecturer' ? 'bg-gradient-to-br from-primary-500 to-indigo-600 shadow-primary-500/30' : 'bg-gradient-to-br from-emerald-500 to-teal-600 shadow-emerald-500/30'}`}>
                                            {(currentUser.full_name || currentUser.username).charAt(0).toUpperCase()}
                                        </div>
                                        <span className={`absolute -bottom-0.5 -right-0.5 w-3.5 h-3.5 rounded-full border-2 border-white dark:border-slate-900 ${currentUser.role === 'lecturer' ? 'bg-amber-400' : 'bg-emerald-400'}`} title={currentUser.role === 'lecturer' ? 'ToÃ n quyá»n (Lecturer)' : 'Quyá»n há»c táº­p (Student)'}></span>
                                    </div>
                                    <div className="text-sm">
                                        <div className="font-semibold text-slate-800 dark:text-white">{currentUser.full_name}</div>
                                        <div className="text-slate-500 dark:text-slate-400 text-xs flex items-center gap-1.5">
                                            <span className={`px-1.5 py-0.5 rounded text-[10px] font-bold uppercase tracking-wide ${currentUser.role === 'lecturer' ? 'bg-primary-100 text-primary-700 dark:bg-primary-950/60 dark:text-primary-300' : 'bg-emerald-100 text-emerald-700 dark:bg-emerald-950/60 dark:text-emerald-300'}`}>
                                                {roleLabel}
                                            </span>
                                            <span className="hidden md:inline">@{currentUser.username}</span>
                                        </div>
                                    </div>
                                    <button
                                        onClick={handleLogout}
                                        title="ÄÄƒng xuáº¥t"
                                        className="w-9 h-9 rounded-full flex items-center justify-center bg-slate-100 dark:bg-slate-800 text-slate-500 hover:text-rose-600 dark:text-slate-400 dark:hover:text-rose-400 hover:bg-rose-50 dark:hover:bg-rose-950/30 border border-slate-200 dark:border-slate-700 hover:scale-110 active:scale-95 transition-all duration-300 shadow-sm ml-1"
                                    >
                                        <Icon name="right-from-bracket" />
                                    </button>
                                </div>
                            </div>
                        </header>

<div className="p-6 lg:p-10 pb-24 print:p-0">
{activeMenu === 'lookup' ? <LookupScreen /> : activeMenu === 'learning' ? <LearningScreen /> : activeMenu === 'library' ? <LibraryScreen /> : activeMenu === 'history' ? (currentUser && currentUser.role === 'student' ? <StudentHistoryScreen currentUser={currentUser} /> : <HistoryScreen currentUser={currentUser} />) : activeMenu === 'assignment' ? <StudentPortal currentUser={currentUser} /> : <GeneratorScreen />}
                        </div>
                    </main>
                </div>
            );
        };

        const root = ReactDOM.createRoot(document.getElementById('root'));
        root.render(<App />);
    </script>
</body>
</html>
